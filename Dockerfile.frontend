FROM nginx:alpine

# Copy static frontend files into nginx's serve directory
COPY frontend/ /usr/share/nginx/html/

# Copy custom nginx config so /api/* proxies to the backend
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80